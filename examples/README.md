# Example Flows

Import any JSON file in this folder into Node-RED (`Import -> Clipboard`).

Before running any flow, open each `gemini86-sendgrid` node and set the API key credential to:

`SG.YOUR_SENDGRID_API_KEY`

These examples intentionally use placeholder e-mail addresses and generic data.
